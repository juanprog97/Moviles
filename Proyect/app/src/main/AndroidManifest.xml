<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.aplicacionmoviluniversidad">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_sello"
            android:label="Aplicacion Universidad Javeriana"
            android:roundIcon="@mipmap/ic_sello_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
                android:name=".Controladores.login_Activity"
                android:label="Javeriana Cali"
                android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Controladores.cursos_Activity"></activity>
        <activity android:name=".Controladores.MainActivity">
        </activity>
    </application>

</manifest>